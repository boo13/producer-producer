---
phase: 02-window-system
plan: 01
type: execute
---

<objective>
Create reusable .window component with title bar, window controls, and body sections.

Purpose: Establish the core window component that all retro desktop windows will use—the hero form window, decorative windows, and dialogs.
Output: CSS classes for .window, .window-title-bar, .window-controls, .window-body in styles.css.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

# Auto-selected based on dependency graph:
@.planning/phases/01-foundation/01-01-SUMMARY.md

# Key files:
@css/styles.css
@Ref/Ref1.jpg

**Tech stack available:** CSS custom properties from Phase 1
**Established patterns:** CSS variables in :root, kebab-case classes, 4-space indentation
**Constraining decisions:**
- Phase 1: CSS custom properties define palette (--window-bg, --window-border, --title-bar, --accent, --shadow)
- Phase 1: Modern browsers only (no vendor prefixes needed)

**Design reference (Ref1.jpg):**
- Windows have cream/beige background (#f5f0e1 = --window-bg)
- Dark 2px borders (#2a2a2e = --window-border)
- Title bars slightly darker cream (#d4cfc3 = --title-bar)
- Window controls: red close button (#e74c3c = --accent), minimize/maximize buttons
- Hard drop shadow (4px 4px 0 = --shadow)
- Small border radius (4px = --radius-sm)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base .window component CSS</name>
  <files>css/styles.css</files>
  <action>
Add .window class after the .desktop class in styles.css:

```css
.window {
    background: var(--window-bg);
    border: var(--border-width) solid var(--window-border);
    border-radius: var(--radius-sm);
    box-shadow: var(--shadow);
    overflow: hidden;
}
```

This creates the base window container using CSS variables from Phase 1. The overflow:hidden ensures child content respects border-radius.
  </action>
  <verify>Open css/styles.css and confirm .window class exists with background, border, border-radius, box-shadow, and overflow properties</verify>
  <done>.window class defined using CSS variables from :root</done>
</task>

<task type="auto">
  <name>Task 2: Add .window-title-bar with controls</name>
  <files>css/styles.css</files>
  <action>
Add title bar styles after .window:

```css
.window-title-bar {
    background: var(--title-bar);
    padding: var(--spacing-sm) var(--spacing-md);
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    border-bottom: var(--border-width) solid var(--window-border);
}

.window-title {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--ink);
    flex: 1;
}

.window-controls {
    display: flex;
    gap: var(--spacing-xs);
}

.window-control {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    border: 1px solid var(--window-border);
    background: var(--window-bg);
}

.window-control.close {
    background: var(--accent);
    border-color: #c0392b;
}
```

The title bar has:
- Slightly darker background than window body
- Flex layout with controls on left, title grows to fill space
- 12px circular control buttons (close is red, others are neutral)
- Bottom border to separate from body
  </action>
  <verify>Open css/styles.css and confirm .window-title-bar, .window-title, .window-controls, .window-control classes exist</verify>
  <done>Title bar classes defined with proper layout and control button styles</done>
</task>

<task type="auto">
  <name>Task 3: Add .window-body content area</name>
  <files>css/styles.css</files>
  <action>
Add window body style after the title bar styles:

```css
.window-body {
    padding: var(--spacing-md);
}
```

Simple padding for content. Individual windows can override or extend this as needed. The background is inherited from .window (--window-bg).
  </action>
  <verify>Open css/styles.css and confirm .window-body class exists with padding</verify>
  <done>.window-body class defined with content padding</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] css/styles.css has .window class with background, border, shadow
- [ ] css/styles.css has .window-title-bar with flex layout
- [ ] css/styles.css has .window-controls and .window-control classes
- [ ] css/styles.css has .window-body class
- [ ] All new classes use CSS variables from :root (no hardcoded colors)
- [ ] No CSS errors (valid syntax)
</verification>

<success_criteria>
- All 3 tasks completed
- Window component CSS ready for use
- Uses CSS variables from Phase 1
- Follows codebase conventions (kebab-case, 4-space indent)
</success_criteria>

<output>
After completion, create `.planning/phases/02-window-system/02-01-SUMMARY.md`
</output>
