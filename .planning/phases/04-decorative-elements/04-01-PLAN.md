---
phase: 04-decorative-elements
plan: 01
type: execute
---

<objective>
Add decorative dialog windows to create the retro desktop collage aesthetic.

Purpose: Build visual personality around the hero window with supporting decorative elements that reinforce the vintage OS theme without distracting from the subscription form.
Output: Search window, system error dialog, and speech bubble positioned around the hero window.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries (dependency chain):
@.planning/phases/02-window-system/02-01-SUMMARY.md
@.planning/phases/03-hero-form/03-01-SUMMARY.md

# Current source files:
@index.html
@css/styles.css

# Design reference:
@Ref/Ref1.jpg

**Tech stack available:**
- CSS custom properties (--window-bg, --window-border, --title-bar, --accent, --shadow, --radius-sm, spacing variables)
- .window component with title bar and controls

**Established patterns:**
- Window component: .window > .window-title-bar + .window-body
- Scoped component styles: .component-name .element
- CSS variables in :root for consistent theming

**Constraining decisions:**
- Phase 2: Controls positioned left of title (macOS-style)
- Phase 2: 12px circular control buttons with red close accent
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add search window with search bar</name>
  <files>index.html, css/styles.css</files>
  <action>
    Add a decorative search window using the .window component pattern. Structure:
    - .window.search-window positioned absolute (will be placed in Phase 5)
    - Title bar with "Search..." text
    - Window body containing a styled search input with magnifying glass icon (use SVG or CSS pseudo-element)
    - Search input should be decorative (disabled or readonly) - not functional
    - Use existing CSS variables for colors and spacing
    - Add role="presentation" since decorative

    Position initially: top-right area of desktop, rotated slightly with transform: rotate(2deg)
  </action>
  <verify>Browser shows search window with title bar, controls, and search input styled consistently with hero window</verify>
  <done>Search window renders with proper window chrome, search input visible, positioned in desktop area</done>
</task>

<task type="auto">
  <name>Task 2: Add system error dialog</name>
  <files>index.html, css/styles.css</files>
  <action>
    Add a small system error dialog window. Structure:
    - .window.error-dialog (smaller than other windows)
    - Title bar with "System error" text
    - Window body with error message text
    - Two buttons styled as retro dialog buttons: "oh no" and "ok"
    - Buttons should have subtle 3D effect (inset/outset borders) for retro feel
    - Use existing CSS variables, add --button-face: #d4d0c8 if needed for 3D button look
    - Add role="dialog" and aria-label for accessibility

    Position: overlapping other elements slightly, rotated with transform: rotate(-3deg)
  </action>
  <verify>Browser shows error dialog with title bar, message text, and two clickable-looking buttons</verify>
  <done>Error dialog renders with retro button styling, proper ARIA attributes, positioned with slight rotation</done>
</task>

<task type="auto">
  <name>Task 3: Add lost message speech bubble</name>
  <files>index.html, css/styles.css</files>
  <action>
    Add a speech bubble element (not using .window component). Structure:
    - .speech-bubble element with "LOST MESSAGE" or similar text
    - Use CSS for bubble shape: rounded rectangle with triangle pointer
    - Triangle pointer created with CSS borders (::after pseudo-element)
    - Background: var(--window-bg), border: var(--border-width) solid var(--window-border)
    - Bold, uppercase text for retro aesthetic
    - Add role="presentation" since decorative

    Position: bottom area of desktop, slight rotation
  </action>
  <verify>Browser shows speech bubble with pointer, styled text, positioned in desktop layout</verify>
  <done>Speech bubble renders with CSS triangle pointer, consistent styling with window palette</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All three decorative elements visible in browser
- [ ] Elements use consistent color palette (CSS variables)
- [ ] Window elements follow .window component pattern
- [ ] ARIA attributes present on dialog elements
- [ ] No console errors
- [ ] Hero window subscription form still functional
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Search window, error dialog, and speech bubble render correctly
- Decorative elements don't interfere with form functionality
- Page still passes basic accessibility (no unlabeled interactive elements)
</success_criteria>

<output>
After completion, create `.planning/phases/04-decorative-elements/04-01-SUMMARY.md`
</output>
