---
phase: 04-decorative-elements
plan: 02
type: execute
---

<objective>
Add desktop icons and widget decorations to complete the retro desktop collage.

Purpose: Fill out the desktop metaphor with folder icons, a media player widget, and loading bar - creating depth and visual interest while maintaining the vintage OS aesthetic.
Output: Folder icons column, media player widget, and loading bar positioned around the desktop.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summaries:
@.planning/phases/02-window-system/02-01-SUMMARY.md
@.planning/phases/04-decorative-elements/04-01-SUMMARY.md

# Current source files:
@index.html
@css/styles.css

# Design reference:
@Ref/Ref1.jpg

**Tech stack available:**
- CSS custom properties (all palette and spacing variables)
- .window component with title bar and controls
- Decorative elements from 04-01 (search window, error dialog, speech bubble)

**Established patterns:**
- Window component: .window > .window-title-bar + .window-body
- Scoped component styles: .component-name .element
- role="presentation" for decorative elements
- CSS transforms for rotation effects

**Constraining decisions:**
- Decorative elements use existing CSS variables for consistency
- Non-interactive elements marked with appropriate ARIA
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add folder icons column</name>
  <files>index.html, css/styles.css</files>
  <action>
    Add a vertical column of folder icons mimicking desktop icons. Structure:
    - .folder-column container for layout
    - 3-4 .folder-icon items, each with:
      - Folder shape (CSS or simple SVG - prefer CSS with border/background for consistency)
      - Label text below (Documents, Photos, Memories, etc.)
    - Folder shape: rectangle with tab at top-left corner (classic folder silhouette)
    - Use var(--window-bg) for folder color, var(--window-border) for outline
    - Icons arranged vertically with gap
    - Add role="presentation" on container since decorative

    Position: left side of desktop, vertically stacked
  </action>
  <verify>Browser shows folder icons with labels, styled consistently with window palette</verify>
  <done>Folder icons column renders with 3-4 folders, each labeled, positioned on left side of desktop</done>
</task>

<task type="auto">
  <name>Task 2: Add media player widget</name>
  <files>index.html, css/styles.css</files>
  <action>
    Add a decorative media player using the .window component. Structure:
    - .window.media-player (compact size)
    - Title bar (can omit controls or include them)
    - Window body containing:
      - Time display (e.g., "5:08" - static text)
      - Waveform visualization (CSS bars or simple graphic representation)
      - Playback controls row: prev, play/pause, next buttons
      - Controls are decorative buttons (not functional)
    - Waveform: series of vertical bars with varying heights, use CSS with multiple divs or pseudo-elements
    - Control buttons: circular or rounded with play/pause icons (CSS triangles or simple text)
    - Use existing CSS variables for colors

    Position: right side of desktop, slight rotation
  </action>
  <verify>Browser shows media player with time, waveform bars, and control buttons</verify>
  <done>Media player widget renders with all visual elements (time, waveform, controls), styled consistently</done>
</task>

<task type="auto">
  <name>Task 3: Add loading bar decoration</name>
  <files>index.html, css/styles.css</files>
  <action>
    Add a decorative loading/progress bar element. Structure:
    - .loading-bar container (not using .window - standalone element)
    - Outer track with border and background
    - Inner progress fill (partial width, e.g., 60-70%)
    - Optional: "Loading..." text above or beside
    - Track: var(--window-bg) background, var(--window-border) border
    - Fill: var(--accent) or var(--title-bar) for contrast
    - Subtle 3D inset effect on track (box-shadow: inset)
    - Add role="presentation" since decorative

    Position: bottom area of desktop, horizontal orientation
  </action>
  <verify>Browser shows loading bar with track and partial fill, styled with retro aesthetic</verify>
  <done>Loading bar renders with progress fill, consistent styling, positioned in desktop layout</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Folder icons visible with labels
- [ ] Media player shows time, waveform, and controls
- [ ] Loading bar shows track with partial fill
- [ ] All elements use consistent color palette
- [ ] No console errors
- [ ] Hero window subscription form still functional
- [ ] Page layout accommodates all decorative elements
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Desktop has full complement of decorative elements
- Visual cohesion maintained across all elements
- Form functionality preserved
- Phase 4 complete, ready for Phase 5 (Responsive & Polish)
</success_criteria>

<output>
After completion, create `.planning/phases/04-decorative-elements/04-02-SUMMARY.md`
</output>
