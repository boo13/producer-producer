---
phase: 03-hero-form
plan: 01
type: execute
---

<objective>
Build the hero window containing title, description, and subscription form using the window component system.

Purpose: Transform the current white-card container into a retro desktop window while preserving the fully functional Buttondown subscription form.
Output: Hero window with integrated form, styled to match retro desktop aesthetic.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/02-window-system/02-01-SUMMARY.md

# Key files:
@index.html
@css/styles.css

**Tech stack available:** CSS custom properties, .window component, .desktop wrapper
**Established patterns:**
- Window component: .window > .window-title-bar + .window-body
- Control buttons: .window-controls > .window-control.close/.minimize/.maximize
- CSS variables for colors (--window-bg, --ink, --accent) and spacing (--spacing-*)

**Constraining decisions:**
- Phase 2: Controls positioned left of title (macOS-style)
- Phase 2: 12px circular control buttons with red close accent
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure HTML to use window component</name>
  <files>index.html</files>
  <action>Replace the existing .container div with .window structure:

  1. Change `<div class="container">` to `<div class="window hero-window">`
  2. Add window title bar structure at the top:
     ```html
     <div class="window-title-bar">
       <div class="window-controls">
         <span class="window-control close"></span>
         <span class="window-control minimize"></span>
         <span class="window-control maximize"></span>
       </div>
       <span class="window-title">producer-producer</span>
     </div>
     ```
  3. Wrap the existing content (image, h1, description, form) in `<div class="window-body">`
  4. Remove the header image (img.img-head) - the window title bar replaces visual hierarchy
  5. Keep the h1.title, p.description, and form exactly as-is (preserve Buttondown integration)

  Do NOT modify the form element, its attributes, or form field names - the existing JavaScript depends on them.</action>
  <verify>HTML structure matches: .window > .window-title-bar + .window-body > (h1 + p + form). Browser shows window with title bar.</verify>
  <done>HTML uses window component structure; form remains functional; no JavaScript errors in console</done>
</task>

<task type="auto">
  <name>Task 2: Style hero window and form for retro aesthetic</name>
  <files>css/styles.css</files>
  <action>Update styles to integrate form into retro window aesthetic:

  1. Add .hero-window class:
     - width: 400px (or max-width with % for responsive)
     - Center text alignment for content

  2. Restyle .title (h1):
     - Use --ink color
     - Remove uppercase if desired, or keep per preference
     - Appropriate size for window context

  3. Restyle .description:
     - Use --ink-light color
     - Appropriate margins

  4. Restyle .form-box:
     - Stack vertically (grid or flex-direction: column)
     - Use spacing variables
     - Remove hardcoded margins

  5. Restyle .input-main:
     - Use --window-bg or slightly lighter for input background
     - Use --window-border for subtle border
     - Remove pill shape (border-radius: var(--radius-sm))
     - Remove margin-right

  6. Restyle .submit button:
     - Replace gradient with solid --accent background
     - Use --radius-sm for subtle rounding (not pill shape)
     - Hover state: slightly darker

  7. Remove or update the old .container styles (no longer used)

  Use CSS variables throughout - no hardcoded colors or spacing values.
  Do NOT modify .phone-field (honeypot) styles - they must remain hidden.</action>
  <verify>npm run build or direct browser refresh shows styled hero window with form matching retro aesthetic</verify>
  <done>Form styled with CSS variables; no hardcoded colors; input and button match window aesthetic; submit button uses accent color</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Hero window with integrated subscription form styled for retro desktop aesthetic</what-built>
  <how-to-verify>
    1. Open index.html in browser (or run local server)
    2. Verify window appearance:
       - Cream/beige background
       - Dark border with drop shadow
       - Title bar with three control buttons (red close button on left)
       - Title text "producer-producer" in title bar
    3. Verify form functionality:
       - Enter email address
       - Click Submit
       - Confirm form submits to Buttondown (or shows validation if empty)
       - Check no console JavaScript errors
    4. Verify visual integration:
       - Form doesn't look out of place in window
       - Colors harmonize (no jarring contrasts)
       - Spacing feels balanced
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Window component structure in HTML (window > title-bar + body)
- [ ] Form submits successfully to Buttondown
- [ ] No JavaScript console errors
- [ ] All colors use CSS variables (no hardcoded hex except inherited legacy)
- [ ] Visual appearance matches retro desktop aesthetic
</verification>

<success_criteria>

- Hero window displays with proper title bar and controls
- Subscription form is fully functional
- Form styling uses CSS variables
- No regression in form spam protection (honeypot still hidden)
- Human visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-form/03-01-SUMMARY.md` following summary template with frontmatter.
</output>
