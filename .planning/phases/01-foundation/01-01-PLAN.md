---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Establish the retro desktop visual foundation with CSS custom properties, global reset, and dark slate background.

Purpose: Create the core visual system that all subsequent phases build on—color palette, spacing, typography baseline, and the desktop metaphor wrapper.
Output: Updated styles.css with CSS variables and desktop background, updated index.html with .desktop wrapper structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@css/styles.css
@index.html
@Ref/Ref1.jpg

**Tech stack:** Pure HTML/CSS, no build tools
**Established patterns:** kebab-case classes, 4-space indentation, media queries at end of file
**Design reference:** Ref/Ref1.jpg shows dark gray (#2a2a2e or similar) desktop background with cream/beige (#f5f0e1 or similar) window elements
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define CSS custom properties for retro palette</name>
  <files>css/styles.css</files>
  <action>
Add `:root` block at the top of styles.css (after the @import for fonts) with CSS custom properties:

**Colors:**
- --desktop: #3d3d3d (dark slate gray for background)
- --window-bg: #f5f0e1 (cream/beige for window backgrounds)
- --window-border: #2a2a2e (dark border color)
- --title-bar: #d4cfc3 (slightly darker cream for title bars)
- --accent: #e74c3c (red for window controls/accents)
- --ink: #2a2a2e (text color)
- --ink-light: #636363 (secondary text)

**Sizing:**
- --border-width: 2px
- --shadow: 4px 4px 0 rgba(0, 0, 0, 0.3)
- --radius-sm: 4px
- --spacing-xs: 4px
- --spacing-sm: 8px
- --spacing-md: 16px
- --spacing-lg: 24px

Keep the existing @import for Google Fonts at the very top. Add :root immediately after.
  </action>
  <verify>Open css/styles.css and confirm :root block exists with all variables defined</verify>
  <done>:root block with 7 color variables and 7 sizing variables present in styles.css</done>
</task>

<task type="auto">
  <name>Task 2: Update body/global styles with desktop background</name>
  <files>css/styles.css</files>
  <action>
Replace the existing body background (gradient) with the desktop background:

1. Change body background from `linear-gradient(45deg, #fb4764, #f88d5f)` to `var(--desktop)`
2. Remove the vendor-prefixed background-size rules (-webkit-, -moz-, -o-)
3. Keep body background-size: cover (still useful for future)
4. Add min-height: 100vh to body to ensure full coverage
5. Update the * reset to include box-sizing: border-box (already there, keep it)

The body should now be a solid dark slate color.
  </action>
  <verify>Open index.html in browser, confirm background is dark gray (not pink/orange gradient)</verify>
  <done>Body displays dark slate background (#3d3d3d), no gradient visible</done>
</task>

<task type="auto">
  <name>Task 3: Add .desktop wrapper to HTML structure</name>
  <files>index.html, css/styles.css</files>
  <action>
**In index.html:**
1. Wrap the existing `<div class="container">` element inside a new `<main class="desktop">`
2. Structure becomes: `<main class="desktop"><div class="container">...</div></main>`

**In css/styles.css:**
Add .desktop class styles:
```css
.desktop {
    min-height: 100vh;
    padding: var(--spacing-lg);
    display: flex;
    justify-content: center;
    align-items: flex-start;
    padding-top: 50px;
}
```

This creates the semantic wrapper for the desktop metaphor while keeping the existing container centered.
  </action>
  <verify>Open index.html in browser, confirm page still renders with content centered on dark background, container still visible</verify>
  <done>.desktop wrapper in HTML, .desktop CSS class defined, existing container still visible and centered</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] css/styles.css has :root block with CSS custom properties
- [ ] Body background is dark slate (var(--desktop))
- [ ] index.html has `<main class="desktop">` wrapper
- [ ] .desktop class styled in CSS
- [ ] Page renders without errors in browser
- [ ] Existing form still visible and functional
</verification>

<success_criteria>
- All 3 tasks completed
- CSS custom properties defined for colors and sizing
- Desktop background replaces gradient
- HTML restructured with .desktop wrapper
- No visual regression on form functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
